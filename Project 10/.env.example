# ─── DeployPilot Environment Configuration ────────────────────────────────────
# Copy this file to .env and fill in real values before starting the application.
# NEVER commit .env to version control.

# Application
APP_NAME=DeployPilot
APP_VERSION=1.0.0
APP_ENV=development           # development | staging | production
DEBUG=true
SECRET_KEY=change-me-to-a-256-bit-random-value-before-production
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080

# Database (PostgreSQL)
DATABASE_URL=postgresql+asyncpg://deploypilot:deploypilot@localhost:5432/deploypilot
DATABASE_POOL_SIZE=10
DATABASE_MAX_OVERFLOW=20
DATABASE_ECHO=false

# Redis / Celery
REDIS_URL=redis://localhost:6379/0
CELERY_BROKER_URL=redis://localhost:6379/1
CELERY_RESULT_BACKEND=redis://localhost:6379/2

# JWT
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=60
JWT_REFRESH_TOKEN_EXPIRE_DAYS=30

# GitHub App Integration
GITHUB_APP_ID=
GITHUB_APP_PRIVATE_KEY=
GITHUB_WEBHOOK_SECRET=
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# AWS
AWS_REGION=us-east-1
AWS_SECRETS_MANAGER_PREFIX=deploypilot

# Terraform
TERRAFORM_BINARY_PATH=/usr/local/bin/terraform
TERRAFORM_WORKING_DIR=/tmp/deploypilot/runs
TERRAFORM_STATE_BUCKET=

# Email (SMTP)
SMTP_HOST=smtp.sendgrid.net
SMTP_PORT=587
SMTP_USERNAME=apikey
SMTP_PASSWORD=
SMTP_FROM_ADDRESS=no-reply@deploypilot.io

# Slack
SLACK_DEFAULT_WEBHOOK=

# Rate Limiting
RATE_LIMIT_REQUESTS=100
RATE_LIMIT_WINDOW_SECONDS=60

# Observability
LOG_LEVEL=INFO
LOG_FORMAT=json
SENTRY_DSN=
PROMETHEUS_ENABLED=true

# Storage
S3_BUCKET_ARTIFACTS=
S3_BUCKET_LOGS=

# Initial admin (seeding)
INITIAL_ADMIN_EMAIL=admin@deploypilot.io
INITIAL_ADMIN_PASSWORD=ChangeMe123!
