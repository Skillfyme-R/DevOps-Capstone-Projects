apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: deploypilot

resources:
  - namespace.yaml
  - api-deployment.yaml
  - worker-deployment.yaml
  - ingress.yaml

commonLabels:
  app.kubernetes.io/name: deploypilot
  app.kubernetes.io/part-of: deploypilot
  app.kubernetes.io/managed-by: kustomize
