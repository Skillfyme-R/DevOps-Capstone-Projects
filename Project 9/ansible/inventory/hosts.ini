[eks_nodes]
# EKS worker nodes — populated dynamically by AWS Systems Manager inventory
# Use SSM Session Manager for remote access rather than direct SSH

[medicart_dev]
# Development environment managed nodes

[medicart_staging]
# Staging environment managed nodes

[medicart_prod]
# Production environment managed nodes — requires change approval

[all:vars]
aws_region=us-east-1
ecr_registry=ACCOUNT_ID.dkr.ecr.us-east-1.amazonaws.com
company=MediCart Health Technologies Pvt. Ltd.
platform=medicart-platform
