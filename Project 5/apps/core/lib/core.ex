defmodule Core do
  @moduledoc """
  Core domain logic for FluxStream — the enterprise OTT streaming platform.

  This umbrella app contains all business logic, Ecto schemas, background
  service abstractions, and shared infrastructure clients used across the
  FluxStream platform.
  """
end
